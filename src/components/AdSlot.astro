---
const { label = 'Advertisement', format = 'display' } = Astro.props;
---
<aside class={`ad-slot ad-slot--${format}`} aria-label={label}><span>{label}</span><small>AdSense reserved area</small></aside>
