import { listPublished } from '../lib/articles';
import type { PublicArticle } from '../lib/types';
export const GET = async ({ locals, site }: { locals: App.Locals; site?: URL }) => { let items: PublicArticle[] = []; try { items = await listPublished(locals.runtime.env, 50); } catch {} const origin = String(site || 'https://example.com').replace(/\/$/, ''); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>TrustWire Korea</title><link>${origin}</link><description>Latest news</description>${items.map((a) => `<item><title><![CDATA[${a.title}]]></title><link>${origin}/news/${a.slug}</link><description><![CDATA[${a.dek}]]></description><pubDate>${a.published_at || ''}</pubDate></item>`).join('')}</channel></rss>`, { headers: { 'content-type': 'application/rss+xml; charset=utf-8' } }); };
